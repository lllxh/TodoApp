<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jason.mdtodo">

<!--    获取网络权限-->
    <uses-permission android:name="android.permission.INTERNET"/>
<!--    获取震动权限-->
    <uses-permission android:name="android.permission.VIBRATE" />
<!--    获取闹钟权限-->
    <uses-permission android:name="android.permission.SET_ALARM"/>
    <application
        android:name="com.jason.mdtodo.MdTodoApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MdTally"
        android:usesCleartextTraffic="true">
        <activity android:name="com.jason.mdtodo.AboutMeActivity"></activity>
        <activity android:name="com.jason.mdtodo.NewTodoActivity" />
        <activity
            android:name="com.jason.mdtodo.MainActivity"
            android:label="MdTodo">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>